# Entrega Brandbook Verifica Pix

## Abrir diretamente

Abra `index.html` no navegador.

## Servir localmente

No Terminal:

```bash
cd "/Volumes/SSD-Nilton/[01] VERIFICA PIX /Branding book /ENTREGA_BRANDBOOK_VERIFICAPIX_HTML"
python3 -m http.server 8080
```

Depois acesse `http://localhost:8080`.

O projeto usa HTML, CSS, JavaScript mínimo e SVG. Não requer instalação.
