# Integracao ClickUp

O MVP usa adapter mockado. A implementacao real deve mapear Workspace, Space,
Folder e List, salvar `clickup_task_id`, usar idempotencia e registrar cada sync.

As variaveis necessarias estao em `painel/.env.example`.
